name: Release

# Publish BonsAI.app whenever a v* tag is pushed (or via manual dispatch). The app is unsigned
# (ad-hoc, by the toolchain) and not notarized, so users clear quarantine on first launch — but every
# release ships an EdDSA-signed appcast.xml so installed copies auto-update and relaunch seamlessly
# via Sparkle.
#
# Required repository secret (one-time owner setup — see docs/releasing.md):
#   SPARKLE_PRIVATE_KEY   the Sparkle EdDSA private key (from `generate_keys -x`)
# The matching Sparkle EdDSA *public* key is committed in script/build_and_run.sh (SPARKLE_PUBLIC_KEY).
# This is Sparkle's own update-integrity key — NOT Apple code signing.

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build & release (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Build & publish
    runs-on: macos-26
    env:
      REPO: ${{ github.repository }}
      TAG: ${{ github.event.inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Swift toolchain
        run: swift --version

      - name: Detect Apple signing
        id: signing
        env:
          CERT: ${{ secrets.MACOS_CERT_P12_BASE64 }}
        run: |
          if [ -n "$CERT" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Apple signing secrets absent — this release will be UNSIGNED (notarization skipped)."
          fi

      - name: Import Developer ID certificate
        if: steps.signing.outputs.enabled == 'true'
        env:
          MACOS_CERT_P12_BASE64: ${{ secrets.MACOS_CERT_P12_BASE64 }}
          MACOS_CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"
          KEYCHAIN_PW="kc-${{ github.run_id }}"
          echo "$MACOS_CERT_P12_BASE64" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security create-keychain -p "$KEYCHAIN_PW" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PW" "$KEYCHAIN_PATH"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN_PATH" -P "$MACOS_CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PW" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH" login.keychain-db
          rm -f "$RUNNER_TEMP/cert.p12"

      # Reuses the same staging path developers run locally, minus the launch. BONSAI_VERSION pins the
      # bundle's CFBundleShortVersionString/CFBundleVersion to the tag (what Sparkle compares). When
      # signing is enabled, notarize.sh also Developer ID-signs (incl. Sparkle's helpers), notarizes,
      # staples, and packages the final BonsAI.app.zip + .sha256.
      - name: Build, sign & notarize BonsAI.app
        env:
          SIGNING_ENABLED: ${{ steps.signing.outputs.enabled }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          export BONSAI_VERSION="${TAG#v}"
          if [ "$SIGNING_ENABLED" = "true" ]; then
            # notarize.sh signs + notarizes the app (→ BonsAI.app.zip for Sparkle) and the
            # drag-to-install BonsAI.dmg.
            NOTARY_PROFILE= ./script/notarize.sh
          else
            ./script/build_and_run.sh bundle
            ditto -c -k --sequesterRsrc --keepParent dist/BonsAI.app dist/BonsAI.app.zip
            shasum -a 256 dist/BonsAI.app.zip | tee dist/BonsAI.app.zip.sha256
            # Still ship a drag-to-install dmg; it just isn't notarized in this fallback path.
            DMG_NOTARIZE=false ./script/make_dmg.sh
          fi

      - name: Assemble release notes
        env:
          SIGNING_ENABLED: ${{ steps.signing.outputs.enabled }}
        run: |
          version="${TAG#v}"
          # Lift the matching section out of the changelog (everything between this version's heading
          # and the next "## " heading), trimming leading blanks.
          awk -v v="$version" '
            index($0, "## [" v "]") == 1 { grab = 1; next }
            grab && index($0, "## ") == 1 { exit }
            grab { print }
          ' CHANGELOG.md | sed -e '/./,$!d' > dist/RELEASE_NOTES.md
          if ! grep -q '[^[:space:]]' dist/RELEASE_NOTES.md; then
            if [ "$SIGNING_ENABLED" = "true" ]; then
              cat > dist/RELEASE_NOTES.md <<EOF
          Signed & notarized macOS build of **BonsAI** ($TAG). Download **BonsAI.dmg**, then drag **BonsAI** onto **Applications** — no right-click or quarantine workaround needed.

          BonsAI updates itself automatically (also **BonsAI ▸ Check for Updates…**).
          EOF
            else
              cat > dist/RELEASE_NOTES.md <<EOF
          Unsigned macOS build of **BonsAI** ($TAG).

          This build is not notarized. On first launch, right-click the app and choose **Open**, or clear the quarantine flag:

          \`\`\`
          xattr -dr com.apple.quarantine /Applications/BonsAI.app
          \`\`\`

          After that, BonsAI updates itself automatically (also **BonsAI ▸ Check for Updates…**).
          EOF
            fi
          fi

      # Build the EdDSA-signed appcast Sparkle reads. sign_update emits the ready-to-embed
      # `sparkle:edSignature="…" length="…"` attributes for the enclosure. (Sparkle's own integrity
      # key — not Apple code signing.)
      # Sparkle shows the appcast <description> in a WKWebView as HTML — raw markdown reads as
      # literal ### and ** mush. Render the notes through GitHub's own markdown renderer and wrap
      # them in a minimal, dark-mode-aware stylesheet so the update dialog shows real release notes.
      - name: Render release notes to HTML
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BODY_HTML=$(gh api markdown -f mode=gfm -f text="$(cat dist/RELEASE_NOTES.md)") || BODY_HTML=""
          if [ -z "$BODY_HTML" ]; then
            # Renderer unreachable — fall back to readable preformatted text rather than HTML-mangled markdown.
            BODY_HTML="<pre style=\"white-space:pre-wrap\">$(sed -e 's/&/\&amp;/g' -e 's/</\&lt;/g' dist/RELEASE_NOTES.md)</pre>"
          fi
          cat > dist/RELEASE_NOTES.html <<HTML
          <!DOCTYPE html>
          <html><head><meta charset="utf-8"><style>
            :root { color-scheme: light dark; }
            body { font: 13px/1.55 -apple-system, 'Helvetica Neue', sans-serif; margin: 14px 16px;
                   color: #1d1d1f; }
            h1, h2, h3 { font-size: 13px; font-weight: 600; margin: 14px 0 4px;
                         text-transform: uppercase; letter-spacing: 0.04em; opacity: 0.55; }
            ul { margin: 4px 0 10px; padding-left: 20px; }
            li { margin: 3px 0; }
            p { margin: 6px 0; }
            code { font: 11.5px ui-monospace, 'SF Mono', Menlo, monospace;
                   background: rgba(127,127,127,0.14); border-radius: 4px; padding: 1px 4px; }
            pre { background: rgba(127,127,127,0.14); border-radius: 6px; padding: 8px 10px;
                  overflow-x: auto; }
            pre code { background: none; padding: 0; }
            a { color: #0969da; text-decoration: none; }
            @media (prefers-color-scheme: dark) {
              body { color: #e8e8ed; }
              a { color: #6cb2ff; }
            }
          </style></head><body>
          $BODY_HTML
          </body></html>
          HTML

      - name: Generate appcast.xml
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          version="${TAG#v}"
          SIGN_UPDATE=$(find .build/artifacts -name sign_update -path '*/bin/*' | head -1)
          if [ -z "$SIGN_UPDATE" ]; then echo "::error::sign_update not found in .build/artifacts" >&2; exit 1; fi
          if [ -z "$SPARKLE_PRIVATE_KEY" ]; then echo "::error::SPARKLE_PRIVATE_KEY secret is not set — see docs/releasing.md." >&2; exit 1; fi
          # Write the secret (the `generate_keys -x` export) to a file and sign via --ed-key-file.
          # (sign_update's -s string flag is deprecated and rejects this key format.)
          KEYFILE="$RUNNER_TEMP/sparkle_ed_key"
          printf '%s' "$SPARKLE_PRIVATE_KEY" > "$KEYFILE"
          SIGN_ATTRS=$("$SIGN_UPDATE" --ed-key-file "$KEYFILE" dist/BonsAI.app.zip)
          rm -f "$KEYFILE"
          PUBDATE=$(date -u +"%a, %d %b %Y %H:%M:%S +0000")
          # Keep the CDATA section intact even if the rendered notes ever contain "]]>".
          NOTES=$(sed 's/]]>/]]\&gt;/g' dist/RELEASE_NOTES.html)
          cat > dist/appcast.xml <<XML
          <?xml version="1.0" encoding="utf-8"?>
          <rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
          <channel>
          <title>BonsAI</title>
          <link>https://github.com/$REPO/releases/latest/download/appcast.xml</link>
          <item>
          <title>BonsAI $version</title>
          <pubDate>$PUBDATE</pubDate>
          <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
          <sparkle:fullReleaseNotesLink>https://github.com/$REPO/releases</sparkle:fullReleaseNotesLink>
          <description><![CDATA[
          $NOTES
          ]]></description>
          <enclosure url="https://github.com/$REPO/releases/download/$TAG/BonsAI.app.zip" sparkle:version="$version" sparkle:shortVersionString="$version" $SIGN_ATTRS type="application/octet-stream" />
          </item>
          </channel>
          </rss>
          XML
          echo "--- appcast.xml ---"
          cat dist/appcast.xml

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          assets=(dist/BonsAI.dmg dist/BonsAI.dmg.sha256 dist/BonsAI.app.zip dist/BonsAI.app.zip.sha256 dist/appcast.xml)
          if gh release view "$TAG" --repo "$REPO" >/dev/null 2>&1; then
            gh release upload "$TAG" --repo "$REPO" --clobber "${assets[@]}"
          else
            gh release create "$TAG" --repo "$REPO" \
              --title "BonsAI $TAG" \
              --notes-file dist/RELEASE_NOTES.md \
              "${assets[@]}"
          fi
